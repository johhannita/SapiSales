//
// Created by Johanna on 3/1/2022.
//
#include "lab2.h"
void testUserFunctionalities(){
    ///After lab2 this need to be commnted because the birth year of the user is changed to BirthDate struct
    /*//Declaring user pointers
    User *user1, *user2, *user3;

    ///Creating users
    createU(&user1);
    createU(&user2);
    createU(&user3);

    ///Fill with data
    getUserData(user1, "Michael Scott", TEACHER, MALE, MATHEMATICS_INFORMATICS, 1962);
    getUserData(user2, "Jim Halpert", STUDENT, MALE, INFORMATICS, 1979);
    getUserData(user3, "Pam Beesly", STUDENT, FEMALE, TELECOMMUNICATION, 1974);


    ///Print user
    printUser(user1);
    printUser(user2);
    printUser(user3);

    ///Delete users
    deleteUser(&user1);
    deleteUser(&user2);
    deleteUser(&user3);}


void testProductFunctionalities(){
    ///Create products
    Product *product1, *product2, *product3;
    createP(product1);
    createP(product2);
    createP(product3);

    getProductData(product1,"MS1", "Apple", FRUIT, 10);
    getProductData(product2,"MS2", "Book", SCHOOL, 4);
    getProductData(product3,"MS3", "Tomato", GROCERY, 20);

    printProduct(product1);
    printProduct(product2);
    printProduct(product3);

    deleteProduct(product1);
    deleteProduct(product2);
    deleteProduct(product3);*/
}